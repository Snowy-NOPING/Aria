.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Apple's Developer Font Licence allows SF Pro in an interface but not
# redistribution, and this repository is public. The files stay on disk for
# local builds; see static/fonts/NOTICE.md. Figtree (OFL) is committed.
static/fonts/SFPro*.woff2

# Agent tooling and machine-local settings, not part of the app.
.claude/
.agents/
